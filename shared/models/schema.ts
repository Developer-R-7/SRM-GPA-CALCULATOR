export interface searchBarScehma {
  placeholder: string;
}

export interface heroSectionSchema {
  title: string;
  description: string;
}

export interface listSchema {
  cource_title: string;
  credit: string;
  grade: string;
}
